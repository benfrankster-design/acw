# Key People — ACW

ACW is a single-operator project at present. This file is sparse by design.

## Operator

- **Ben Frank (`benfrankster`)** — author and sole operator. Identity context in `~/.claude/CLAUDE.md` and `~/synapse/Rules/Identity/about-me.md`.

## Reviewers / collaborators

(None as of v0.6.0. ACW has not been opened to external collaborators; the canonical review process is the operator's own session-by-session work plus subagent stress tests where named in build-log entries.)

## Downstream instance operators

(Same operator; multi-instance setup is single-human across `cs-copilot`, `_Command`, plus ACW itself. When a second human operator adopts ACW, this section grows.)

## Reference humans (cited in research)

- See `research/sources.md` and `LINEAGE.md` for prior-art authors and the research-thread context. Not "key people" in the operational sense; cited for accountability.
